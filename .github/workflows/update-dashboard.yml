name: YouTube dashboard - home PC mode

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "Automatic updates are performed by Windows Task Scheduler on the home PC."
          echo "This cloud workflow does not fetch YouTube transcripts."
